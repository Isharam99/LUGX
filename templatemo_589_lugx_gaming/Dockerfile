# Use a standard Nginx web server
FROM nginx:alpine

# Copy all project files from the current folder into the server's web root
COPY . /usr/share/nginx/html/

# Expose port 80 for web traffic
EXPOSE 80